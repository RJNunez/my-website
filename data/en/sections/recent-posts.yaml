# section information
section:
  name: Blog
  id: recent-posts
  enable: false
  weight: 5
  showOnNavbar: true
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required